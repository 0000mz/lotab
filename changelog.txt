2025-Dec-26:
- Multiple tab selection.
- Close multiple tabs.
- Unify active and non-active tabs in ui list.

2025-Dec-25:
- When the GUI is escaped, return focus to last focused application before the interruption.
- Show all tabs and active tab in GUI.
- Added socket tests.
- Added context to log lines.

2025-Dec-22:
- Hide the GUI until the keybind is triggered.
- Do not make the icon show up in the MacOS taskbar when the GUI is shown.
- Always center the GUI when it appears and make it unmoveable.
- When the GUI is in focus and visible, any click out of focus should hide the application.
- Hide the window controls on the GUI.
- Have the daemon show its active at the top-right of the MacOS.
- daemon: Combine the sanitizer header into the config.h.
- GUI testing with AppleScript.
- Make daemon engine unit-testable.
- Separate the daemon engine from the OS interactions to make unit-testable.
- Setup clang format and use raw-string-literal decoration support.
- Add loglevel to daemon to reduce spam of weboscket messages.
- Create log function that will automatically handle whether or not to log based on loglevel.
- Refactor engine and daemon binary to internalize each component.

2025-Dec-21:
- Initialize project
  - Create browser extension for forwarding browser tab events.
  - Create daemon for handling websocket server and controlling the
    application GUI.
- Start prototype GUI SwiftUI (inside app/ dir).
- Using Unix Domain Sockets (UDS) to communicate b/w the daemon and the GUI.
- Using Carbon to register global keybind (Ctrl+Shift+J for now) to trigger program callback.
- Daemon controls the process of the TabManager GUI.

TODO:
- Restrict triggering of global keybind to only browser sessions.
- Use case 1: creation of new task
  - Sketch out workflow of creating a new task and assigning tabs to each task.
- Use nullability annotation and in/out annotation for C function parameters.
- Use C attributes to annotate sync objects.
- Figure out how to make the websocket connection happen all the time on startup. Currently,
  the extension's service worker sometimes need to be woken up.
- Treat tabs.onHighlight as tab activation.
- When in search mode, do not make the list selectable or navigateable until the search is
  confirmed or canceled.
- Color/label tabs that are associated with the currently focused browser window.
- SHIFT+X to close all-but selected tabs.
- Add confirmation for serious actions such as closing multiple tabs.
- SHIFT+A to select all tabs.
